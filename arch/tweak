#!/bin/bash

sudo pacman -Sy

echo 'changing scale and font dpi'
sh <(curl -sSL gmanka.tk/arch/scale)

echo 'changing fonts'
sh <(curl -sSL gmanka.tk/arch/fonts)

echo 'configuring konsole'
sh <(curl -sSL gmanka.tk/arch/konsole/tweak)
