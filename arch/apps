#!/bin/bash

no_color='\033[0m'
green='\033[1;32m'

echo -e "$green uninstalling libre office$no_color"
sudo pacman --noconfirm -Rs "$(pacman -Qsq | grep libreoffice)"

sudo pacman --noconfirm -Sy steam python-pip telegram neofetch micro git

sudo pip install -U pip

paru --noconfirm -Sy inxi
